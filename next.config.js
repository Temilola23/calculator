/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any specific Next.js configurations here if needed.
  // For a basic calculator, default settings are usually sufficient.
};

module.exports = nextConfig;